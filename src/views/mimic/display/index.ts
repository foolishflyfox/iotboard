import type { DisplayData } from '@mimic/types';

/** 创建图纸时默认的配置 */
export const initDisplayData: DisplayData = {
  baseMap: {
    backgroundColor: '#00BFFF50',
    sizeType: 'custom',
    width: 500,
    height: 300,
  },
};
